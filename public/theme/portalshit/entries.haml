.articles.section
  - cache_fragment('index-categories', shared: true) do
    %ul#categories
      - Category.all.each do |category|
        %li= link_to category.title, "/category/#{category.slug}/"
  - @posts.each_with_index do |post, index|
    = partial :article, :locals => {:post => post, :index => index}
  - cache_fragment('index-pager', shared: true) do
    = @posts.pager.to_html(request.url)
