.amazon
  -if @error
    %p.error= @error
  -else
    .amazon-image
      = link_to image_tag(@image), @link
    .amazon-content
      %ul
        %li= link_to @title, @link
        %li= @author.present? ? @author : @manufacturer
        %li= @price
